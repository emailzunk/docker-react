sudo: required
language: generic
services:
    docker

before-install:
    - docker build -t emailzunk/docker-react-dev -f Dockerfile.dev .

script:
    - docker run -e CI=true emailzunk/docker-react-dev npm run test
